# funciones.py

import numpy as np

def grados_a_radianes(grados):
  """Convierte ángulos en grados a radianes."""
  return np.radians(grados)

def radianes_a_grados(radianes):
  """Convierte ángulos en radianes a grados."""
  return np.degrees(radianes)